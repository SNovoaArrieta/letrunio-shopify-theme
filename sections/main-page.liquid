<section class="section content-page">
  <div class="page-width">
    <h1>{{ page.title }}</h1>
    <div class="richtext">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Página principal", "settings": [] }
{% endschema %}
