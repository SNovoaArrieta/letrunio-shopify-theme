<section class="section content-page">
  <div class="page-width content-page-wrap">
    <header class="content-page-heading">
      <p class="eyebrow">LETRUNIO</p>
      <h1>{{ page.title }}</h1>
    </header>
    <article class="richtext content-card">{{ page.content }}</article>
  </div>
</section>

{% schema %}
{ "name": "Página principal", "settings": [] }
{% endschema %}
